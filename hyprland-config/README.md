# Custom Hyprland Config

Folder ini berisi konfigurasi Hyprland yang sudah saya kustomisasi.

> **Catatan:** File konfigurasi original (default) biasanya berada di `~/.config/`.

~/.config/caelestia
~/.config/hypr
